# A placeholder: every scenario that applies this overlay patches config.yaml
# with the configuration it needs, so what stands here is never what runs.
apiVersion: v1
kind: ConfigMap
metadata:
  name: profgate-pgo
data:
  config.yaml: |
    placeholder: true
