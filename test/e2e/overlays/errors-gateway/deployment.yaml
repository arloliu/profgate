apiVersion: apps/v1
kind: Deployment
metadata:
  name: profgate-errors
  labels:
    app.kubernetes.io/name: profgate-errors
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: profgate-errors
  template:
    metadata:
      labels:
        app.kubernetes.io/name: profgate-errors
    spec:
      serviceAccountName: profgate-errors
      terminationGracePeriodSeconds: 120
      containers:
        - name: profgate
          image: ko.local/profgate:e2e
          args:
            - serve
            - --config
            - /etc/profgate/config.yaml
          ports:
            - name: api
              containerPort: 8080
            - name: ops
              containerPort: 9090
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /readyz
              port: ops
          volumeMounts:
            - name: config
              mountPath: /etc/profgate
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: profgate-errors
