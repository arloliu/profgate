version: "2"
linters:
  default: standard
  enable: [bodyclose, errorlint, exhaustive, gosec, misspell, noctx, revive]
  settings:
    govet:
      enable: [stdversion]
formatters:
  enable: [gofmt, goimports]
