auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    mapping:
      users:
        - name: ci-bot
          realm: nobody
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
