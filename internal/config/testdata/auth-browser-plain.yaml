auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    mapping:
      defaultRealm: developer
    browser:
      clientID: profgate
      redirectURL: https://profgate.example/auth/callback
      cookieKeyFile: testdata/cookie.key
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
