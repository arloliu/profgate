auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    mapping:
      groups:
        - name: platform
          realm: developer
      defaultRealm: ""
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
