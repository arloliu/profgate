auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    mapping:
      defaultRealm: developer
    cli:
      clientId: profgate
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
