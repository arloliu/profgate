auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    mapping:
      defaultRealm: developer
    cli:
      scopes: ["openid", "email"]
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
