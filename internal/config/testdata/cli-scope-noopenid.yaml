auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    mapping:
      defaultRealm: developer
    cli:
      scopes: ["offline_access"]
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
