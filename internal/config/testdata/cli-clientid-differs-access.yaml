auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    tokenType: access
    mapping:
      defaultRealm: developer
    cli:
      clientID: other
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
