server:
  tls:
    certFile: testdata/tls.crt
    keyFile: testdata/tls.key
auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    mapping:
      defaultRealm: developer
    browser: {}
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
