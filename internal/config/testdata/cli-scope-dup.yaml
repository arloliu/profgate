auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    mapping:
      defaultRealm: developer
    cli:
      scopes: ["openid", "offline_access", "offline_access"]
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
