server:
  tls:
    certFile: testdata/tls.crt
    keyFile: testdata/tls.key
auth:
  mode: basic
  basic:
    maxConcurrent: 4
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
