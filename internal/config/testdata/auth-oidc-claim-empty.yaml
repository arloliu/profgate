auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    groupsClaim: ""
    mapping:
      defaultRealm: developer
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
