server:
  tls:
    certFile: testdata/tls.crt
    keyFile: testdata/tls.key
auth:
  mode: basic
  basic:
    users:
      - name: alice
        passwordHash: "$1$abcdefgh$0123456789012345678901"
        realm: developer
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
