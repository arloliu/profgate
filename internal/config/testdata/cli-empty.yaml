auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    mapping:
      defaultRealm: developer
    cli: {}
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
