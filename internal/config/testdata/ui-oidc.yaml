auth:
  mode: oidc
  oidc:
    issuer: https://issuer.example
    audience: profgate
    mapping:
      defaultRealm: developer
ui:
  enabled: true
realms:
  developer:
    namespaces: ["*"]
    services: ["*"]
    profiles: ["*"]
