// Package ui serves the embedded operator console at /ui/:
// the vendored browser libraries and the console's own assets, each at a stable path under /ui/,
// and the shell at /ui/ that mounts them.
package ui
